import { A11yRadioGroupDirective } from './radio-group.directive';

describe('RadioGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new A11yRadioGroupDirective();
    expect(directive).toBeTruthy();
  });
});
