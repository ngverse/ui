import { A11yTabGroupDirective } from './tab-group.directive';

xdescribe('TabGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new A11yTabGroupDirective();
    expect(directive).toBeTruthy();
  });
});
