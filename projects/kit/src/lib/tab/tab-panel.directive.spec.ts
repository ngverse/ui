import { A11yTabPanelDirective } from './tab-panel.directive';

xdescribe('TabPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new A11yTabPanelDirective();
    expect(directive).toBeTruthy();
  });
});
