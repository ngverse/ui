import { A11yOptionDirective } from './option.directive';

xdescribe('OptionDirective', () => {
  it('should create an instance', () => {
    const directive = new A11yOptionDirective();
    expect(directive).toBeTruthy();
  });
});
