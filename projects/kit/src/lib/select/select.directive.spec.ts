import { A11ySelectDirective } from './select.directive';

xdescribe('SelectDirective', () => {
  it('should create an instance', () => {
    const directive = new A11ySelectDirective();
    expect(directive).toBeTruthy();
  });
});
