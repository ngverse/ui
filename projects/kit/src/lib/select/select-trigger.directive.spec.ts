import { A11ySelectTriggerDirective } from './select-trigger.directive';

describe('SelectTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new A11ySelectTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
