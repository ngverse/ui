import { A11yOptionGroupDirective } from './option-group.directive';

xdescribe('OptionGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new A11yOptionGroupDirective();
    expect(directive).toBeTruthy();
  });
});
