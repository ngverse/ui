import { A11yCheckboxDirective } from './checkbox.directive';

describe('CheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new A11yCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
