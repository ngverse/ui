import { A11yAlertDirective } from './alert.directive';

describe('AlertDirective', () => {
  it('should create an instance', () => {
    const directive = new A11yAlertDirective();
    expect(directive).toBeTruthy();
  });
});
