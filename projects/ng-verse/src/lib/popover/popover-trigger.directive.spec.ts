import { PopoverTriggerDirective } from './popover-trigger.directive';

describe('PopoverNewTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
