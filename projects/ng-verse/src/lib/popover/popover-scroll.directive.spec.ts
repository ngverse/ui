import { PopoverScrollDirective } from './popover-scroll.directive';

describe('PopoverScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverScrollDirective();
    expect(directive).toBeTruthy();
  });
});
