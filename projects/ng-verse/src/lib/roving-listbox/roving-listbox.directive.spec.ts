import { RovingListboxDirective } from './roving-listbox.directive';

xdescribe('RovingListboxDirective', () => {
  it('should create an instance', () => {
    const directive = new RovingListboxDirective();
    expect(directive).toBeTruthy();
  });
});
