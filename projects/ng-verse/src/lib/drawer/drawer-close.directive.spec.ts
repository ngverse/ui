import { DrawerCloseDirective } from './drawer-close.directive';

describe('DrawerCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerCloseDirective();
    expect(directive).toBeTruthy();
  });
});
