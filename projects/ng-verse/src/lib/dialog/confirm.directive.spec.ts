import { ConfirmDirective } from './confirm.directive';

xdescribe('ConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
