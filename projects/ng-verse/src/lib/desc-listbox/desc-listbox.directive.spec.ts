import { DescListboxDirective } from './desc-listbox.directive';

xdescribe('DescListboxDirective', () => {
  it('should create an instance', () => {
    const directive = new DescListboxDirective();
    expect(directive).toBeTruthy();
  });
});
