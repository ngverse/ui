import { DescListboxItemDirective } from './desc-listbox-item.directive';

xdescribe('DescListboxItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DescListboxItemDirective();
    expect(directive).toBeTruthy();
  });
});
