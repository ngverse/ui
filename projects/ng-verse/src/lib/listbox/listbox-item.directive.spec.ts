import { ListboxItemDirective } from './listbox-item.directive';

describe('ListboxItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ListboxItemDirective();
    expect(directive).toBeTruthy();
  });
});
